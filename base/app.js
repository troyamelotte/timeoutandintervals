function runMe(){
  console.log("Ran!");
}

// Runs a function after a given time frame
setTimeout(runMe, 2000);

// Runs a function after a given time frame repetitively
// setInterval(runMe, 1000);
